<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.5/css/bulma.css">
    <style>
        #sidebar {
            background-color: #f5f5f5; 
            overflow-y: scroll;
            height: 100vh;
            border-right: 1px solid #e5e5e5;
        }
        .perfil-item, .perfil-item.logout button {
            display: block;
            text-decoration: none;
            color: inherit;
            padding: 1rem;
            border-bottom: 1px solid #e5e5e5;
            cursor: pointer;
            background: none;
            border: none;
            width: 100%;
            text-align: left;
            font-family: inherit;
            font-size: inherit;
        }
        .perfil-item:hover, .perfil-item.logout:hover button {
            background-color: #e9e9e9;
        }
        .logout, .logout button {
            color: red;
        }
        .logout:hover, .logout:hover button {
            background-color: red;
            color: white;
        }
        .active-item {
          background-color: #dcdcdc;
        }
    </style>

<div id="sidebar" class="column is-3">
    <%= link_to "Usuarios", user_reports_path, id: "user-reports", class: "perfil-item #{'active-item' if current_page?(user_reports_path)}" %>
    <%= link_to "Productos", edit_user_registration_path, id: "seguridad-link", class: "perfil-item #{'active-item' if current_page?(edit_user_registration_path)}" %>
</div>


