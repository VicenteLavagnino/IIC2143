<!DOCTYPE html>
<html>
<head>
  <title>Detalle del Producto</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.5/css/bulma.css">
  <style>
    .image-container img {
      width: 100%; 
      height: 100%;
      object-fit: contain; 
      object-position: center;
    }
  </style>
</head>
<body>
  <section class="section">
  <%= form_with(model: [@product, @offer], local: true) do |f| %>
    <div class="field">
      <%= f.label :name, "Nombre de la Oferta", class: "label" %>
      <%= f.text_field :name, class: "input" %>
    </div>
  
    <div class="field">
      <%= f.label :description, "Descripción de la Oferta", class: "label" %>
      <%= f.text_area :description, class: 'textarea' %>
    </div>
  
    <div class="field">
      <%= f.label :image, "URL de la imagen", class: 'label' %>
      <%= f.text_field :image, class: 'input' %>
    </div>
  
    <div class="field">
      <%= f.label :state, "Estado de la Oferta", class: 'label' %>
      <%= f.text_field :state, class: 'input' %>
    </div>
  
    <div class="field">
      <%= f.submit "Crear Oferta", class: 'button is-primary' %>
    </div>
  <% end %>
  
  </section>
</body>
</html>
