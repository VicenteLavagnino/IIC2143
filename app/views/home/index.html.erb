<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.5/css/bulma.css">
    <style>
        /* Estilos previos */
        .card {
            box-shadow: 0 2px 3px rgba(10, 10, 10, 0.1), 0 0 0 1px rgba(10, 10, 10, 0.1);
            border-radius: 10px;
            background-color: #f5f5f5;
        }

        .title {
            color: #00d1b2;
            font-size: 3rem;
        }

        .card-header-title {
            color: #00d1b2;
            font-size: 2rem;
            background-color: #f5f5f5;
        }

        .notification.is-warning {
            background-color: #ffdd57;
            border-radius: 10px;
        }

        .button.is-primary {
            background-color: #00d1b2;
            border-color: transparent;
            border-radius: 10px;
        }

        .button.is-primary:hover {
            background-color: #008c76;
        }

        /* Estilos para la sección de desplazamiento horizontal */
        .scrolling-wrapper {
            overflow-x: auto;
            white-space: nowrap;
            padding: 20px 0;
            background-color: #f5f5f5;
        }

        .scrolling-card {
            display: inline-block;
            width: 200px;
            margin-right: 10px;
            cursor: pointer;
        }

        .card-content {
            padding: 10px;
        }

        .card-image img {
            max-width: 100%;
            height: auto;
        }

        .title-section {
            background-color: #eeeeee;
            padding: 10px;
            border-radius: 10px;
            margin-bottom: 10px;
            color: #00d1b2;
            font-size: 3rem;
        }

        .catalogue-bar {
            text-align: center; /* Centra el contenido de la barra */
            padding: 20px 0; /* Espacio arriba y abajo para la barra */
            background-color: #f5f5f5; /* Color de fondo igual al de las tarjetas */
            margin-bottom: 20px; /* Espacio debajo de la barra antes de las tarjetas */
        }

        /* Estilos adicionales para las nuevas secciones */
        .gallery-images img {
            width: calc(18% - 10px); /* Ajusta el tamaño como sea necesario */
            margin: 5px;
            border-radius: 5px;
        }

        faq-item:not(:last-child) {
            margin-bottom: 15px;
        }

        .social-media-icons {
            font-size: 24px;
            margin-right: 10px;
        }

        .footer {
            background-color: #f5f5f5;
            padding: 20px;
            text-align: center;
        }

    </style>
</head>
<body>
    <!-- Sección Hero -->
    <section class="hero">
        <div class="hero-body">
            <div class="container has-text-centered">
                <h1 class="title">
                    ¡Bienvenido a Ba Zar!
                </h1>
                <p class="subtitle">
                    El sitio web de intercambio de LEGOs más grande de Chile.
                </p>
                <%= image_tag('logo.png', alt: 'Logo', width: '75', height: 'auto') %>
            </div>
        </div>
    </section>

    <!-- Barra para revisar el catálogo completo -->
    <div class="has-text-centered">
        <div class="title" style="magin-bottom: 5px">
        <a class="button is-primary" href="/explora">Revisa todo el catálogo</a>
        </div>
    </div>

    <!-- Sección de las últimas publicaciones -->
    <section class="section">
        <div class="container has-text-centered">
            <div class="title-section">
                <h2 class="title is-4">Revisa las últimas publicaciones</h2>
            </div>
            <div class="scrolling-wrapper">
                <% @latest_legos.each do |lego| %>
                    <a href="/explora" class="scrolling-card card">
                        <div class="card-content">
                            <div class="content">
                                <h3><%= lego.name %></h3>
                                <p><%= lego.description %></p>
                                <%= cl_image_tag(lego.image, width: 200, height: 200, crop: "fill") %>
                            </div>
                        </div>
                    </a>
                <% end %>
            </div>
        </div>
    </section>

        <!-- Sección de Galería de Fotos -->
    <section class="section">
        <div class="container has-text-centered">
            <h2 class="title-section">Galería</h2>
            <div class="gallery-images">
                <%= image_tag('lego1.png', alt: 'Imagen 1', width: '18%', height: 'auto') %>
                <%= image_tag('lego2.png', alt: 'Imagen 1', width: '18%', height: 'auto') %>
                <%= image_tag('lego3.png', alt: 'Imagen 1', width: '18%', height: 'auto') %>
                <%= image_tag('lego4.png', alt: 'Imagen 1', width: '18%', height: 'auto') %>
                <%= image_tag('lego5.png', alt: 'Imagen 1', width: '18%', height: 'auto') %>
            </div>
        </div>
    </section>

    <!-- Sección de Contacto -->
    <section class="section">
        <div class="container has-text-centered">
            <h2 class="title-section" href="/contacto">Contacto</h2>
            <!-- Añade aquí tu formulario de contacto o información -->
            <div class="content" style="margin-top: 20px;">
                <p class="subtitle">
                   En caso de cualquier duda o necesidad de ayuda nos puedes contactar por este medio, responderemos a la brevedad.
                <p class="subtitle">
                    Completa el formulario a continuación y nos pondremos en contacto contigo pronto.
                </p>
                <a class="button is-primary" href="/contacto">Contáctanos</a>
            </div>
        </div>
    </section>


    <!-- Sección Sobre Nosotros -->
    <section class="section">
        <div class="container has-text-centered">
            <h2 class="title-section">Sobre Nosotros</h2>
            <p class="subtitle" style="margin-top: 20px;">
                En Ba Zar nos dedicamos a ofrecer una plataforma para que los usuarios puedan intercambiar sus legos.
            </p>
            <p class="subtitle" style="margin-top: 20px;">
                Somos un equipo de excelencia comprometido en ofrecer la mejor experiencia a nuestros usuarios y estamos constantemente mejorando nuestro sitio.
            </p>

            

            <a class="button is-primary" href="/nosotros">Conoce más</a>

        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="content has-text-centered">
            <p>&copy; 2023 Ba Zar. Todos los derechos reservados.</p>
        </div>
    </footer>


</body>
</html>
